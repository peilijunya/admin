<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新增列表</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.css">
    
</head>
<body>
    <script src="/javascripts/jquery-1.11.0.js"></script>
    
    <div class="contaniner">
     
        <div class="panel panel-info">
              <div class="panel-heading">
                    <h3 class="panel-title">新增商品</h3>
              </div>
              <div class="panel-body">
                    
                    <form action="list" method="POST" role="form">
                        <legend>请填写商品信息</legend>
                    
                        <div class="form-group">
                            <label for="">商品名称</label>
                            <input type="text" class="form-control" id="" placeholder="Input field" name="pname">
                        </div>
                       <div class="form-group">
                           <label for="">商品价格</label>
                           <input type="text" class="form-control" id="" placeholder="Input field" name="pprice">
                       </div>
                       <div class="form-group">
                        <label for="">商品图片</label>
                       <input type="file" style="display: none;" class="files">
                       <input type="hidden" id="pimg" name="pimg">
                       <img src="/uploads/1.jpg" style="width: 30px; height: 30px;" class="imgBtn">
                    </div>
                    <script>
                        $('.imgBtn').click(function(){
                            $('.files').click();
                            

                        });
                        $('.files').change(function(){
                            if($(this)[0].files.length>0){
                                let formData=new FormData();
                                formData.append('awr',$(this)[0].files[0]);
                                $.ajax({
                                    type:"post",
                                    url:'upload',
                                    data:formData,
                                    cache:false,
                                    processData:false,
                                    contentType:false,
                                    success:function(res){
                                        console.log(res);
                                        $("#pimg").val(res.file);


                                    }

                                })
                            }
                        })

                    </script>
                       <div class="form-group">
                           <label for="">商品描述</label>
                           <input type="text" class="form-control" id="" placeholder="Input field" name="pdesc">
                       </div>
                    
                        
                    
                        <button type="submit" class="btn btn-primary">提交</button>
                    </form>
                    
              </div>
        </div>
        
   
       </div>
</body>
</html>